import ApiKeySettings from '@/components/api-key-settings';

export const metadata = {
  title: 'Settings - Tio Manolo Cowork',
};

export default function SettingsPage() {
  return <ApiKeySettings />;
}
